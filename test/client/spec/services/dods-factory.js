'use strict';

describe('Service: dodsFactory', function () {

  // load the service's module
  beforeEach(module('dodsApp'));

  // instantiate service
  var dodsFactory;
  beforeEach(inject(function (_dodsFactory_) {
    dodsFactory = _dodsFactory_;
  }));

  // it('should do something', function () {
  //   expect(!!dodsFactory).toBe(true);
  // });

});
